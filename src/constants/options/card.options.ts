const cardOptions = {
  card: {
    fields: [
      {
        name: "title",
        id: 1,
        type: "text",
        value: "title",
        placeholder: "Titulo",
      },
    ],
  },
};

export default cardOptions;
